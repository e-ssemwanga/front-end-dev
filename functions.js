function greet(text){
   
    console.log('welcome to our website' + text);

}

console.log('rest of the code');

function signUp(){
    let name = prompt('what is your name')

greet(name);
}

console.log(name);
signUp();

// the function if done on js ins't run automatically
//it just saves but you can then use it whenever you want
